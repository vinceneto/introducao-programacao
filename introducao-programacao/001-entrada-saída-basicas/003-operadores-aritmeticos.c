#include <stdio.h>

int main()
{
    int a, b, soma, subtracao, multiplicacao, divisao, modulo;

    a = 5;
    b = 8;

    soma = a + b;           // Calcula a soma de a e b
    subtracao = a - b;      // Calcula a diferença de a e b
    multiplicacao = a * b;  // Calcula o produto de a e b
    divisao = a / b;        // Calcula a divisao de a e b
    modulo = a % b;         // Calcula o resto da divisao de a e b

    return 0;
}